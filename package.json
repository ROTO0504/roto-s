{
  "name": "roto-s",
  "version": "0.1.0",
  "private": true,
  "description": "URL shortener for s.roto.work — Cloudflare Workers + D1 + Hono with a Vite/React admin SPA",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "bun@1.3.11",
  "scripts": {
    "dev:api": "bun --cwd apps/api run dev",
    "dev:admin": "bun --cwd apps/admin run dev",
    "build:admin": "bun --cwd apps/admin run build",
    "deploy": "bun run build:admin && bun --cwd apps/api run deploy",
    "typecheck": "bun --cwd apps/api run typecheck && bun --cwd apps/admin run typecheck"
  }
}
